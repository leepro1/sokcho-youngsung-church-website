<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sokchoys.member.dao.MemberDao">

    <select id="findById" parameterType="int" resultType="MemberDto">
        select id, email, name, date_format(createAt, '%Y-%m-%d')
        from members
        where id = #{id}
    </select>

    <select id="findByEmail" parameterType="String" resultType="MemberDto">
        select member_id, email, password, name, role, date_format(createAt, '%Y-%m-%d')
        from members
        where email = #{email}
    </select>

    <select id="findAll" resultType="MemberDto">
        select member_id, email, name, role, date_format(createAt, '%Y-%m-%d')
        from members
    </select>

    <insert id="createMember" parameterType="MemberFormDto">
        insert into members(email, password, name, role)
        values(#{email}, #{password}, #{name}, #{role})
    </insert>

    <update id="updatePassword" parameterType="MemberPwdDto">
        update members set (password = #{password})
        where member_id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        delete from members
        where member_id = #{id}
    </delete>

</mapper>